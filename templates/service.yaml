apiVersion: v1
kind: Service
metadata:
  name: confluent-replicator
  labels:
    app: confluent-replicator
spec:
  type: ClusterIP
  ports:
    - port: 8083
      targetPort: rest
      name: rest
  selector:
    app: confluent-replicator
